<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:LoginApp"
             x:Class="LoginApp.MainPage">

    <StackLayout Padding="10" BackgroundColor="#c4e7ff">

        <Label Text="Inloggning" HorizontalTextAlignment="Center" FontSize="Large" Margin="0, 15"/>

        <Entry Text="{Binding Email}" Placeholder="E-Post" BackgroundColor="#BBFFFFFF" />

        <Entry Text="{Binding Password}" Placeholder="Lösenord" IsPassword="True" BackgroundColor="#CCFFFFFF" />

        <Button Text="Login" Command="{Binding LoginCommand}" Margin="10, 25" BorderRadius="25"/>
    </StackLayout>

</ContentPage>
